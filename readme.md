## 谷歌浏览器扩展

用于覆盖新标签页并可自定义初始地址，点击扩展图标也可在新标签页打开该地址。

## 使用方式

1. 下载仓库
1. 打开 Chrome，访问 chrome://extensions/
1. 开启右上角的 开发者模式
1. 点击 **加载已解压的扩展程序**，选择 startpage-extension 文件夹
1. 安装后：
    * 按 Ctrl+T（新标签）会自动跳转到你设置的初始地址。
    * 点击扩展图标可以打开弹出窗口并用按钮在新标签打开该地址。
    * 右键扩展图标 → “扩展程序选项” 或者扩展弹窗里的链接都可以打开设置页面修改地址。
